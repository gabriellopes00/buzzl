describe('Add User Route', () => {
  // const psqlHelper = new PsqlConnection()
  // beforeAll(async () => await psqlHelper.connect())
  // afterAll(async () => await psqlHelper.close())
  // beforeEach(async () => {
  //   await psqlHelper.getConnection().getCustomRepository(PsqlUserRepository).delete({})
  // })

  // it('Should create a user return 200 on success', async () => {
  //   await request(app).post('/signup').send(fakeUserParams).expect(200)
  // })

  it('', () => {
    expect(1).toBe(1)
  })
})
