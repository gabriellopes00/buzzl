describe('Middlewares Setup', () => {
  // it('Should return default content type as json', async () => {
  //   app.get('/fake_route', (req, res) => res.send(''))
  //   await request(app).get('/fake_route').expect('content-type', /json/)
  // })

  it('', () => {
    expect(1).toBe(1)
  })
})
